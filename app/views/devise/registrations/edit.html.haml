.settings-container
  .settings-console
    .settings-graybox
      .settings-logo
        %img(src="http://i.imgur.com/tTRdWNT.gif")
  = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "settings-right", method: :put }) do |f|
    = devise_error_messages!
    %h2.settings-head Account Settings
    %p.settings-p Change e-mail address
    = f.email_field :email, autofocus: true, class: "change-email", placeholder: "Enter new email address"
    %p.settings-p Change password
    = f.password_field :password, autocomplete: "off", class: "change-password", placeholder: "Enter new password"
    = f.password_field :password_confirmation, autocomplete: "off", class: "change-password", placeholder:"Enter new password again"
    %p.settings-p Confirm current password
    = f.password_field :current_password, autocomplete: "off", class: "settingsconfirm", placeholder: "Confirm password"
    %br
    %br
    %ul.settings-buttons
      %li.settings-submit
        = f.submit "Update", class: "settings-noUnderline"